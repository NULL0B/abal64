/* File generated by /home/abal64/bin/linbda Version 1.1d.0.02 : /home/abal64/src/blaster/audio.def -> /home/abal64/src/blaster/audio.c */
#ifndef __linbda__home_abal64_src_blaster_audio_c__
#define __linbda__home_abal64_src_blaster_audio_c__
#include <stdio.h>
#include <setjmp.h>
#include "ldfbda.h"

static BDA * DynamicLibrary;
	LPBDA_INIT	rt_init;
	BDA_INIT	v_rt_init;
	BDA_ARG rt_arg;

/* ABAL Runtime Error Trap */
static jmp_buf rt_error_context;
VOID rt_error(EXAWORD evalue) { 
	longjmp(rt_error_context,evalue);
	return;
}

static int function_counter=0;
/* ABAL Word Convertor */
static EXAWORD LdfAbalByte(char FAR PTR bptr)
{
	return(*bptr);
}

static EXAWORD LdfAbalWord(BYTE FAR PTR wptr)
{
	return(((*wptr<< 8) | *(wptr+1)));
}

static EXAWORD LdfAbalLong(BYTE FAR PTR wptr)
{
	return((((*wptr)<< 24) | (*(wptr+1) << 16)  | (*(wptr+2) << 8)| *(wptr+3)));
}

#define LdfParaWord(n) ( tptr[n] == 1 ? LdfAbalWord(pptr[n]) : ( tptr[n] == 0 ?  LdfAbalByte(pptr[n]) : LdfAbalLong(pptr[n])) )
#define LdfParaByte(n) ( tptr[n] == 1 ? (LdfAbalWord(pptr[n])&0x00FF) : ( tptr[n] == 0 ?  LdfAbalByte(pptr[n]) : (LdfAbalLong(pptr[n])&0x00FF) ) )
/* Library Information */
EXAWORD LdfBdaInformation(void * vptr)
{
	printf(
		"\n   Abal Dynamic Library : AUDIO32.abal3.2b.0.01"
		);
	printf(
		"\n   Copyright (c) Amenesik / Sologic \n\n"
		);
	return(0);
}

/* Library Destructor */
EXAWORD LdfBdaTerminate()
{
	if (DynamicLibrary->fin != (VOID (FAR PTR)()) 0)
		 (*DynamicLibrary->fin)();
	return(0);
}

/* Library Call Through */
EXAWORD LdfBdaExecute(
	EXAWORD fid,
	EXAWORD argc,
	BYTE FAR tptr[],
	VOID FAR PTR FAR pptr[],
	EXAWORD FAR wptr[],
	EXAWORD FAR * eptr)
{
	EXAWORD	result=0;
	rt_arg.c=argc; rt_arg.v=pptr;
	rt_arg.l=wptr; rt_arg.t=tptr;
	if ((result=setjmp(rt_error_context))!=0) {
		if ( eptr != (EXAWORD FAR *) 0) *eptr = result;
		return( result );
		}
	/* version = 1 */
	/* runtime = "audio" */
	switch ( fid ) {
		case	0 : /* % AudioInitialise(%,%,%) */
			result=(*DynamicLibrary->fonction[0])(
				LdfParaWord(0),
				LdfParaWord(1),
				LdfParaWord(2)); break;
		case	1 : /* % AudioLiberate() */
			result=(*DynamicLibrary->fonction[1])(); break;
		case	2 : /* % AudioPutByte(%) */
			result=(*DynamicLibrary->fonction[2])(
				LdfParaWord(0)); break;
		case	3 : /* % AudioPut(%,$) */
			result=(*DynamicLibrary->fonction[3])(
				LdfParaWord(0),
				pptr[1]); break;
		case	4 : /* % AudioSend(%,$) */
			result=(*DynamicLibrary->fonction[4])(
				LdfParaWord(0),
				pptr[1]); break;
		case	5 : /* % AudioStatus()  */
			result=(*DynamicLibrary->fonction[5])(); break;
		case	6 : /* % AudioReset() */
			result=(*DynamicLibrary->fonction[6])(); break;
		case	7 : /* % AudioTempo(%,%,%) */
			result=(*DynamicLibrary->fonction[7])(
				LdfParaWord(0),
				LdfParaWord(1),
				LdfParaWord(2)); break;
		case	8 : /* % AudioReceive(%,$?) */
			result=(*DynamicLibrary->fonction[8])(
				LdfParaWord(0),
				pptr[1]); break;
		case	9 : /* % AudioGet(%,$?) */
			result=(*DynamicLibrary->fonction[9])(
				LdfParaWord(0),
				pptr[1]); break;
		case	10 : /* % AudioGetByte() */
			result=(*DynamicLibrary->fonction[10])(); break;
		case	11 : /* % AudioSound(%) */
			result=(*DynamicLibrary->fonction[11])(
				LdfParaWord(0)); break;
		case	12 : /* % AudioDmaState() */
			result=(*DynamicLibrary->fonction[12])(); break;
		case	13 : /* % AudioAlsm($?,%) */
			result=(*DynamicLibrary->fonction[13])(
				pptr[0],
				LdfParaWord(1)); break;
		case	14 : /* % AudioLbsm($?)  */
			result=(*DynamicLibrary->fonction[14])(
				pptr[0]); break;
		case	15 : /* % AudioGetMix(%) */
			result=(*DynamicLibrary->fonction[15])(
				LdfParaWord(0)); break;
		case	16 : /* % AudioPutMix(%,%) */
			result=(*DynamicLibrary->fonction[16])(
				LdfParaWord(0),
				LdfParaWord(1)); break;
		case	17 : /* % AudioPutOpl(%,%) */
			result=(*DynamicLibrary->fonction[17])(
				LdfParaWord(0),
				LdfParaWord(1)); break;
		case	18 : /* % AudioOplNote(%,%,%,%) */
			result=(*DynamicLibrary->fonction[18])(
				LdfParaWord(0),
				LdfParaWord(1),
				LdfParaWord(2),
				LdfParaWord(3)); break;
		case	19 : /* % AudioOplMode(%,%) */
			result=(*DynamicLibrary->fonction[19])(
				LdfParaWord(0),
				LdfParaWord(1)); break;
		case	20 : /* % AudioOplCtrl(%) */
			result=(*DynamicLibrary->fonction[20])(
				LdfParaWord(0)); break;
		case	21 : /* % AudioPlay($,%,%) */
			result=(*DynamicLibrary->fonction[21])(
				pptr[0],
				LdfParaWord(1),
				LdfParaWord(2)); break;
		case	22 : /* % AudioRecord($,%,%,%,%) */
			result=(*DynamicLibrary->fonction[22])(
				pptr[0],
				LdfParaWord(1),
				LdfParaWord(2),
				LdfParaWord(3),
				LdfParaWord(4)); break;
		case	23 : /* % AudioTrack(%,$,%,%,%,%) */
			result=(*DynamicLibrary->fonction[23])(
				LdfParaWord(0),
				pptr[1],
				LdfParaWord(2),
				LdfParaWord(3),
				LdfParaWord(4),
				LdfParaWord(5)); break;
		case	24 : /* % AudioStart(%,%) */
			result=(*DynamicLibrary->fonction[24])(
				LdfParaWord(0),
				LdfParaWord(1)); break;
		case	25 : /* % AudioControl(%,%,%,%) */
			result=(*DynamicLibrary->fonction[25])(
				LdfParaWord(0),
				LdfParaWord(1),
				LdfParaWord(2),
				LdfParaWord(3)); break;
		default	: result=138;
		break;
		}
	/* end */
	return(result);
}

/* Library Constructor */

EXAWORD LdfBdaInitialise(
	BDA_INIT FAR PTR descriptor,
	EXAWORD dlength)
{
	int	i=0;
	rt_init = descriptor;
	function_counter=26;
	if (!(DynamicLibrary = InitRelais()))
		return(139);
	else	return(0);
}

#endif  /* __linbda__home_abal64_src_blaster_audio_c__ */
/* End of File */
